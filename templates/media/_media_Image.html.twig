
 <img
         style=
              "
                width:auto;
                max-width: 600px;
                max-height:400px;
                left:0px;
                top:0px;
                overflow:hidden
              "
         src="{{ asset('uploads/images/' ~ mediaEntity.url) }}"
         alt="{{ mediaEntity.name }}"/>

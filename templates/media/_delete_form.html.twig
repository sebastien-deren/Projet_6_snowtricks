<form method="post" action="{{ path('app_media_delete', {'id': mediaEntity.id, 'slug': mediaEntity.figure.slug }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete') }}">
    <button class="btn btn-danger">Delete</button>
</form>

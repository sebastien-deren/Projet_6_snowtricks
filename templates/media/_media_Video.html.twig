<iframe
        style="
        height:200px;
            overflow:hidden"
        frameborder="0"
        src="{{ mediaEntity.url }}"
        title=" Video Player"
        allow="fullscreen"
></iframe>

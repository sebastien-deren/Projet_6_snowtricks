{% extends 'base.html.twig' %}

{% block title %}Edit {{ figure.name }}{% endblock %}

{% block body %}
    <div class="container-lg">

        <div class="d-flex flex-column align-self-center justify-self-center p-3">
            <div class="d-flex justify-content-between my-4 ">
                <div>
                    <a href="{{ path('app_figure_index') }}" class=" btn btn-primary"> << back to list</a>
                </div>
                <div>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        {{ include('figure/_delete_form.html.twig') }}
                    {% endif %}
                </div>
            </div>
            <h1>Edit Figure</h1>

            {{ include('figure/_form.html.twig', {'button_label': 'Update'}) }}


        </div>
    </div>

{% endblock %}
